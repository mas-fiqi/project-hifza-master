import 'package:flutter/material.dart';

class LatihanMakharijScreen extends StatelessWidget {
  const LatihanMakharijScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Latihan Makharijul Huruf')),
      body: const Center(child: Text('Halaman Latihan Makharijul Huruf (dummy)')),
    );
  }
}
