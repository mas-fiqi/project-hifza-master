import 'package:flutter/material.dart';

class UjiSuaraOptionScreen extends StatelessWidget {
  const UjiSuaraOptionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Uji Hafalan (Suara)')),
      body: const Center(child: Text('Halaman Uji Suara (opsi) — logic speech-to-text akan dipindah di sini nanti')),
    );
  }
}
