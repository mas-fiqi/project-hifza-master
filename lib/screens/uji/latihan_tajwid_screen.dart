import 'package:flutter/material.dart';

class LatihanTajwidScreen extends StatelessWidget {
  const LatihanTajwidScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Latihan Tajwid')),
      body: const Center(child: Text('Halaman Latihan Tajwid (dummy)')),
    );
  }
}
