import 'package:flutter/material.dart';

class HafalanKalimatScreen extends StatelessWidget {
  const HafalanKalimatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hafalan Kalimat')),
      body: const Center(child: Text('Halaman Hafalan Kalimat (dummy)')),
    );
  }
}
